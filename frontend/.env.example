# Optional. Leave empty when using Vite dev proxy (backend on localhost:8000).
# For production (e.g. Vercel), set to your deployed backend URL, e.g.:
# VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=
